services:
  # Static Site
  - type: web
    name: neodash
    env: static
    buildCommand: "./build-render.sh"
    staticPublishPath: dist
    routes:
      - route: /(.*)
        path: /index.html
    envVars:
      - key: NODE_ENV
        value: production
